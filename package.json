{
  "name": "lenny-the-lamb",
  "version": "1.0.0",
  "description": "Lenny the Lamb's Gentle Friday - A shared interactive experience",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prebuild": "cd public && npm install --legacy-peer-deps",
    "build": "cd public && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
} 